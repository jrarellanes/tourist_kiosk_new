<%= form_for @emergencyrequest, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :iduser, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :iduser, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :idcorporation, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :idcorporation, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :dateRequest, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_select :dateRequest, :class => 'date_select' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                emergencyrequests_path, :class => 'btn' %>
  </div>
<% end %>
