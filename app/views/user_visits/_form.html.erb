<%= form_for @user_visit, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :user_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :user_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :places_interest_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :places_interest_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :dateVisit, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_select :dateVisit, :class => 'date_select' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :ratig, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :ratig, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                user_visits_path, :class => 'btn' %>
  </div>
<% end %>
